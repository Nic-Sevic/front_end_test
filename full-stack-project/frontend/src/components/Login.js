import {useState} from "react";
import { apiService } from "../services/apiClient";

const Login = () => {
  
    return (
        <div>
            <h1>Login</h1>
        </div>
    );
};

export default Login;
